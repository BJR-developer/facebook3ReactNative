import { StyleSheet } from "react-native";

const topbar = StyleSheet.create({
    container:{
        fontSize:20,
        backgroundColor:'green',
        color:'white',
        paddingVertical:40,
    }
})
export { topbar };