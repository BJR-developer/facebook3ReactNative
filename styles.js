import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mainContainer:{
        backgroundColor:'white',
        color: 'black',
        margin:0,
        shadowRadius:5,
        paddingTop:30,
    }
})
export { styles }